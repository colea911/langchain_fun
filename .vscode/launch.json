{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ice Breaker Runner",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}